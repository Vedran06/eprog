#include <stdio.h>
int main(){
		int i, grades[3];
		float average = 0.0;
		for(i=0; i<3; i++){
			printf("Enter grade %d: ", i+1);
			scanf("%d", &grades[i]); 
		}
		printf("The grades are: \n");
		for(i=0, i<3, i++){
			sum += grades[i];
		}
		average = sum/3;
		printf("The average is: %.2f");
			if ()
		
}
